fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0
pywebpush==1.14.0
python-dotenv==1.0.1
requests==2.31.0
aiofiles==23.2.1
pydantic==2.7.0
# Module 3 — SMS via USB GSM modem (optional, only needed for SMS_METHOD=gammu)
# Windows: pip install python-gammu   Linux/macOS: pip install gammu
# python-gammu; sys_platform == "win32"
# gammu; sys_platform != "win32"
