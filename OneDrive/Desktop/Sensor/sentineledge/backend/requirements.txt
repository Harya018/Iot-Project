fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0
pywebpush==1.14.0
python-dotenv==1.0.1
requests==2.31.0
aiofiles==23.2.1
pydantic==2.7.0

# ── PostgreSQL drivers ────────────────────────────────────────────────────────
psycopg2-binary==2.9.9
asyncpg==0.29.0
databases[asyncpg]==0.9.0

# ── Rate limiting ─────────────────────────────────────────────────────────────
slowapi==0.1.9
limits==3.13.0

# ── Security / password hashing ───────────────────────────────────────────────
bcrypt==4.2.1

# ── USB temperature sensor ────────────────────────────────────────────────────
pyserial==3.5

# ── SMS via USB GSM modem (optional, only for SMS_METHOD=gammu) ───────────────
# Windows: pip install python-gammu   Linux/macOS: pip install gammu
# python-gammu; sys_platform == "win32"
# gammu; sys_platform != "win32"
